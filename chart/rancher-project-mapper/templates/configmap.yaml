apiVersion: v1
data:
  config: |-
    {
      "rules": [
        {
          "regex": "rancher-project-mapper-*",
          "cluster": "c-12345",
          "project": "p-12345"
        }
      ]
    }
kind: ConfigMap
metadata:
  name: rancher-project-mapper
  namespace: cattle-system