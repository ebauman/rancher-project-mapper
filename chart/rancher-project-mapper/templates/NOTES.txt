The Rancher project mapper webhook should now be installed.

A ConfigMap has been created at the following namespace and name:

namespace: {{ .Values.configmap.namespace }}
name: {{ .Values.configmap.name }}

In it, is a sample configuration. Please customize to your needs.